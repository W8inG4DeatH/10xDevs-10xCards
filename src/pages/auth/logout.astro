---
// This page handles logout and redirects to home
// In the future, this will clear the session via API

// For now, just redirect to home page
// When backend is implemented, this will call the logout API endpoint
return Astro.redirect("/");
---
